import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alex Chen - Creative Developer & Digital Artist",
  description: "Portfolio of Alex Chen, a creative developer and digital artist crafting innovative web experiences with cutting-edge technologies and artistic vision.",
  keywords: ["portfolio", "creative developer", "digital artist", "React", "Three.js", "WebGL", "Next.js"],
  authors: [{ name: "Alex Chen" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
